package com.samyprojects.rps.futura_back.security;

public class SecurityConstants {

    // it's in milliseconds so it represents one day
    public static final long JWT_EXPIRATION = 1000*60*60*2;
}
